<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.androidlearning">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="org.litepal.LitePalApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidLearning"
        android:usesCleartextTraffic="true">
        <activity android:name=".CameraAlbumTest.UseAlbumActivity"></activity>
        <activity android:name=".CameraAlbumTest.UseCameraActivity" />
        <activity android:name=".Network.NetworkActivity" />
        <activity android:name=".WebView.WebViewActivity" />
        <activity android:name=".PlayVideo.PlayVideoActivity" />
        <activity android:name=".PlayAudioTest.PlayAudioActivity" />
        <activity android:name=".CameraAlbumTest.CameraAlbumActivity" />
        <activity android:name=".NotificationTest.NotificationActivity" />
        <activity android:name=".NotificationTest.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".LitePal.LitePalActivity" />
        <activity android:name=".ContactsTest.ContactsActivity" />
        <activity android:name=".RuntimePermission.RuntimePermissionActivity" />
        <activity android:name=".MainActivity" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.androidlearning.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
    </application>

</manifest>